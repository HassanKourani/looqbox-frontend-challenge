---
name: use-bun
description: User prefers bun over npm for package management
type: feedback
---

Use `bun` instead of `npm` for installing packages and running scripts.

**Why:** User explicitly corrected npm usage to bun.

**How to apply:** Always use `bun add` instead of `npm install`, `bun run` instead of `npm run`, etc.
